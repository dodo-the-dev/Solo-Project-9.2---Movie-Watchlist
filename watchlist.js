import { renderFilmList } from "/index.js";
window.onload = (e) =>{
  let movieIDs = []
  //TODO: recover array from local storage and iterate over it using renderFilmList method
  for( id of localStorage ){
    JSON.parse(localStorage.getItem)
  }
}